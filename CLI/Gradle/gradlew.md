# gradlew CLI

Help: `./gradlew --help`
Help about `wrapper` command: `gradle help --task wrapper`
Show version: `./gradlew --version`
Update Gradle version: `./gradlew wrapper --gradle-version 7.6`
Add Gradle Wrapper to a project: `gradle wrapper`
