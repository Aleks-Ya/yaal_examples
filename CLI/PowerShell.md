# Windows PowerShell CLI

DNS ([Resolve-DnsName docs](http://resolve-dnsname/))
Resolve DNS name to IP address:
```
Resolve-DnsName -Name www.bing.com
```
Resolve DNS name against specific DNS server:
```
Resolve-DnsName -Name www.bing.com -Server 10.0.0.1
```
