# Astral UV CLI

Site: https://docs.astral.sh/uv/

Install: `brew install uv`

Help: `uv --help`
Help for command: `uv python --help`

List installed Python versions: `uv python list`
Find installed Python version: `uv python find 3.12`
Verify Python version: `uv run -p 3.12 python --version`
