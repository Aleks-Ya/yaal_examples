package ru.yaal.examples.gradle.pitest;

public class MyClass {
    public int math(int a, int b) {
        if (a > 0) {
            return a + b;
        } else {
            return a * b;
        }
    }
}
