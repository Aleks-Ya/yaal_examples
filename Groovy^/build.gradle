subprojects {
    apply plugin: 'groovy'
    sourceSets.main.groovy.srcDir 'src'
    sourceSets.test.groovy.srcDir 'test'

    dependencies {
        compile localGroovy()
        testCompile 'junit:junit:4.11'
    }

    repositories {
        mavenCentral()
    }
}