dependencies {
    def kafkaVersion = "2.8.1"
    def scalaVersion = "2.13"
    implementation "org.apache.kafka:kafka-clients:$kafkaVersion", gsonDep, slf4jSimpleDep,
            "org.apache.avro:avro:1.9.2", "io.confluent:kafka-avro-serializer:5.4.1"
    testImplementation jsonAssertDep,
            "org.apache.kafka:kafka-clients:$kafkaVersion:test",
            "org.apache.kafka:kafka_$scalaVersion:$kafkaVersion",
            "org.apache.kafka:kafka_$scalaVersion:$kafkaVersion:test",
            bouncyCustleDep
}
repositories {
    mavenCentral()
    maven { url 'https://packages.confluent.io/maven/' }
}
sourceSets {
    main {
        java {
            srcDirs += "$buildDir/generated/avro"
        }
    }
}