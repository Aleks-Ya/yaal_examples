String: ${data.string}
Integer: ${data.integer}
Decimal: ${data.decimal}
Boolean (true/false): ${data.boolean?c}
Boolean (custom): ${data.boolean?string("ok", "not ok")}
Date (time): ${data.date?time}
Date (date): ${data.date?date}
Date (datetime): ${data.date?datetime}
