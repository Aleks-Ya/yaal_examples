/**
 * Ипользование АОП Spring на аннотациях AspectJ.
 */
def aspectVersion = '1.8.6'
dependencies {
    compile "org.aspectj:aspectjrt:${aspectVersion}", "org.aspectj:aspectjtools:${aspectVersion}"
}