subprojects {
    dependencies {
        def version = '4.3.7.RELEASE'
        compile "org.springframework:spring-context:$version"
        testCompile "org.springframework:spring-test:$version"
    }
}