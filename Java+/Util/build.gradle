/**
 * Util classes for all other projects.
 */

// Make test jar for using in compileTest
task testJar(type: Jar, dependsOn: testClasses) {
    baseName = "test-${project.archivesBaseName}"
    from sourceSets.test.output
}
configurations {
    tests
}
artifacts {
    tests testJar
}