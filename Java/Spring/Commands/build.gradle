/**
 * Задача: Проинициализировать команды реализациями (паттерн Command) с помощью Spring.
 */
apply plugin : "java"
sourceSets.main.java.srcDir 'src'
dependencies {
    compile 'org.springframework:spring-context:3.2.4.RELEASE'
}
repositories {
    mavenCentral()
}
