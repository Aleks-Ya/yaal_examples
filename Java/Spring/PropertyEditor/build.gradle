/**
 * Задача: проинициализировать объект Date датой 25.04.1984 в контексте Spring (XML).
 */
apply plugin : "java"
sourceSets.main.java.srcDir 'src'
dependencies {
    compile 'org.springframework:spring-context:3.2.4.RELEASE'
}
repositories {
    mavenCentral()
}