[project]
name = "yaal-examples-search"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "rich",
    "pyperclip",
]

[dependency-groups]
dev = [
    "pytest",
    "seedir",
]

[tool.uv]
package = false
