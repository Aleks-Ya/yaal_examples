[project]
name = "thirdparty-aws"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "boto",
    "boto3",
    "aws-cdk-lib",
    "aws-lambda-typing",
    "constructs",
]

[dependency-groups]
dev = [
    "pytest",
]

[tool.uv]
package = false
