[project]
name = "thirdparty-database"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "chromadb",
    "coverage>=7.15.4",
    "qdrant-client",
    "fastembed"
]

[dependency-groups]
dev = [
    "pytest",
]

[tool.uv]
package = false
