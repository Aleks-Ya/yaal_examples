[project]
name = "thirdparty-markdown"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "mistune",
    "markdown2",
    "mdutils",
    "mdformat",
]

[dependency-groups]
dev = [
    "pytest",
]

[tool.uv]
package = false
