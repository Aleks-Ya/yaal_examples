[project]
name = "thirdparty-pandas"
version = "0.1.0"
requires-python = ">=3.12,<3.13"
dependencies = [
    "pandas",
    "numpy",
    "matplotlib",
    "openpyxl",
    "pyarrow",
    "fastparquet",
]

[dependency-groups]
dev = [
    "pytest",
]

[tool.uv]
package = false
